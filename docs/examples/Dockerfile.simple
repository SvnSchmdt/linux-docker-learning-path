FROM nginx:1.25-alpine

# Copy custom HTML into the default nginx webroot
COPY index.html /usr/share/nginx/html/index.html

# Nginx listens on port 80 by default
EXPOSE 80

# nginx image already has a CMD — no need to override it
